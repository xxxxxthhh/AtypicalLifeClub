# Research Radar receipt — 2026-08-21

## 1. Run header

| Field | Value |
|---|---|
| Run date | 2026-08-21 |
| Run timestamp | 2026-08-21 07:02 CST (2026-08-20 19:02 ET, Thursday) |
| Scan window start | 2026-08-19 07:02 CST (2026-08-18 19:02 ET) |
| Scan window end | 2026-08-21 07:02 CST (2026-08-20 19:02 ET) |
| Branch | `codex/research-radar-2026-08-21` |
| Base | `origin/main` @ `8ad81b6` |
| Commit shape | **data commit** — 2 signals, 3 monitoring `latest` updates (2 graded, 1 fail-closed), 4 report Markdown files, feed |

The previous run's scan-window end was recovered from **the 2026-08-20 branch's receipt**, read during housekeeping before the scan: **2026-08-20 07:02 CST (2026-08-19 19:02 ET)**. The contract opens this window 24 hours before that, which is the start recorded above.

**Genuinely new territory versus the 08-20 run.** Everything up to 2026-08-19 19:02 ET was already swept and adjudicated by that run. What is new here is:

- **US / EDGAR:** ET **2026-08-19 19:02 → 2026-08-20 19:02** — the tail of Wednesday's filing day plus all of Thursday.
- **Asia:** CST **2026-08-20 07:02 → 2026-08-21 07:02** — Thursday's full session and evening in Tokyo, Seoul, Shanghai and Hong Kong, plus Friday pre-open.

Both qualifying events landed inside the *new* portion of the window: the Nebius pricing 6-K was accepted by EDGAR at 2026-08-20 02:04 ET, and the Jinpan semi-annual report was uploaded to the SSE disclosure channel at 2026-08-20 18:28 CST. Neither was available to the 08-20 run.

**A note on EDGAR timestamps.** `acceptanceDateTime` in the `data.sec.gov` submissions feed is serialised with a `Z` suffix but is **UTC**, and must be shifted −4h to EDT before being compared with an ET scan window. Applying that shift is what separates the two genuinely new filings from the seven that the 08-20 run had already seen.

---

## 2. Applied this run

Two qualifying chain events, both read in full from the issuer's own filing. No cross-check rule was added; both signals grade under rules that already exist.

### 2.1 Signals added to `signals.json` (2 new, 59 → 61)

| id | date | crossChecks | tickers | reportIds | monitoringRefs |
|---|---|---|---|---|---|
| `jinpan-h1-2026-aidc-orders-vs-operating-cash-flow` | 2026-08-21 | `buildout-orders`, `grid-epc-delivery` | 688676.SS | `jinpan-2026` | `jinpan-2026:aidc-sst-revenue-recognition`, `jinpan-2026:receivables-cash-quality` |
| `nebius-convertible-notes-pricing-upsized-2026-08` | 2026-08-19 | `neocloud-financing` | NBIS | `nebius-2026` | `nebius-2026:funding-durability` |

`jinpan-h1-2026-…` is the **first signal ever logged against `jinpan-2026`**. The report has been in the book since 2026-02-03 with no chain signal attached; its three monitoring items all pointed at this one filing.

### 2.2 Monitoring updates

| report:item | before | after | evidence date |
|---|---|---|---|
| `nebius-2026:funding-durability` | `unclear` @ 2026-08-19 | **`within`** @ 2026-08-19 | 2026-08-19 pricing release (6-K furnished 2026-08-20) |

⚠️ **Read the `nebius-2026:funding-durability` row carefully: the grade flipped but `readingAsOf` did not move, and that is deliberate.** The two grades rest on **two different filings that happen to share a release date**. The prior `unclear` rested on the 6-K furnished 2026-08-19 announcing a *proposed* offering; this `within` rests on the pricing release, which states in terms that "all information in this press release is as of the time of its issuance on August 19, 2026" and was furnished under a separate 6-K on 2026-08-20. `readingAsOf` is set to the date the **evidence** carries, not the date it reached EDGAR, so it correctly stays 2026-08-19. Dating it 2026-08-20 would make the diff easier to read at the cost of misdating the evidence, which is the wrong trade. The accession numbers separate them unambiguously: `0001104659-26-098590` (proposed) versus `0001104659-26-098924` (pricing).
| `jinpan-2026:aidc-sst-revenue-recognition` | (no grade) | **`unclear`** @ 2026-08-21 | 2026 Semi-Annual Report |
| `jinpan-2026:receivables-cash-quality` | (no grade) | **(no grade — fail-closed)** | 2026 Semi-Annual Report |

**Why `nebius-2026:funding-durability` moved to `within`.** The reviewer's 2026-08-20 standing instruction is to grade funding access "only from priced/closed or otherwise binding primary evidence." The offering has now **priced**. The sole stated reason the prior reading was withheld — coupon, conversion price, accretion schedule, final size and settlement all unknown — has been removed by the pricing release. The company priced **and upsized** ($4.50B → $5.00B) into the same market, at a 0.50% coupon on the 2030 tranche with 40.0% / 45.0% initial conversion premia. Graded strictly against this item's own trigger text — *"the '>90% secured' 2026 capex ($20-25B, ~7x revenue) breaks in a tighter capital market"* — that is evidence of continued capital access, not of the frame breaking. Limits recorded in the item and the signal: settlement (2026-08-24) has not occurred and remains subject to customary closing conditions; the $450M / $300M are initial-purchaser options; use of proceeds still lists "general corporate purposes"; $5.0B is a portion of the $20-25B program; the issuer does **not** restate the ">90% secured" percentage, so the grade rests on demonstrated access rather than on re-verification of that ratio; and no yield to maturity is disclosed, so no all-in cost of capital was derived.

**Why `jinpan-2026:aidc-sst-revenue-recognition` is `unclear` and not `within`.** The trigger is a **conjunction of three legs** — *"AIDC, SST, and overseas expansion convert into recognized revenue."* Two legs converted decisively on the issuer's own segment disclosure (AIDC revenue RMB 1.265B, +122.57% YoY; overseas revenue RMB 1.481B, +68.30% YoY). The **SST leg produced no disclosed orders and no recognized revenue at all** — the issuer discloses only a 10kV/2.4MW prototype designed and produced, a first 13.8kV SST prototype shipped overseas for testing, and RMB 11.71M of period R&D spend on that project. Grading `within` would assert a conjunction that one named leg plainly does not satisfy. The grade is withheld because a leg has not begun converting, not for lack of disclosure.

**Why `jinpan-2026:receivables-cash-quality` is fail-closed rather than graded.** See §3.1. This is the run's one blocked edit.

### 2.3 Report metadata

- `nebius-2026.lastUpdate`: 2026-08-20 → **2026-08-21**
- `jinpan-2026.lastUpdate`: 2026-07-31 → **2026-08-21**
- `jinpan-2026:aidc-sst-revenue-recognition.nextCheck` / `nextCheckDate`: "2026 半年报" / `2026-08` → "2026 年三季度报告 / 2026 Q3 report" / **`2026-10`**
- `jinpan-2026:receivables-cash-quality.nextCheck` / `nextCheckDate`: "半年报现金流" / `2026-08` → "2026 年三季度报告的经营活动现金流 / operating cash flow in the 2026 Q3 report" / **`2026-10`**
  - ⚠️ **Self-correction, follow-up commit on this branch.** The first version of both fields read "(SSE statutory deadline 2026-10-31)". **That deadline was inferred, not verified** — no SSE or CSRC rule page was opened this run, and no source was cited for it. Naming a statute and a date inside published display state on an unopened source is exactly the failure this repository's source-citation discipline exists to prevent, so the parenthetical was removed. What remains is supported: a Q3 report is expected because the issuer demonstrably files quarterly (the `annualized-pe` item's own `latest` cites Q1 2026 revenue of RMB 1.523B), and `nextCheckDate` stays at the month precision `2026-10`. **If the reviewer wants a dated deadline in these fields, it needs a cited SSE rule page.**
- **`reportedPeriod` was deliberately NOT changed on `jinpan-2026`** (stays `Q1 2026`). The H1 figures are recorded in monitoring and in the Markdown audit trail, but §3 Financial Health and §8 Valuation remain a Q1-2026-based analysis. Setting `reportedPeriod` to H1 2026 would assert a rebuild that did not happen. That rebuild is proposal **P-7**.
- **`jinpan-2026:annualized-pe` was left entirely untouched** — including its now-stale `nextCheckDate: 2026-08`. It is a valuation item; editing its date without editing its superseded `latest` would produce an internally inconsistent entry, and editing its `latest` is outside this track's authority. The whole item is the subject of **P-7**.

### 2.4 Markdown updated (both languages, aligned facts and dates)

- `Nebius_深度研究报告_2026-07.md` / `Nebius_Deep_Research_Report_2026-07.md` — dated 2026-08-21 entry appended in §9 directly beneath the 08-19 proposed-offering entry, whose own "what to watch next" line asked for exactly these pricing terms. Includes an explicit **current-cycle correction** recording that the $4.50B / $2.75B / $1.75B / $375M figures were the *proposed* caliber and that the post-pricing caliber supersedes them. §8's 2026-07-30 $188.43 valuation caliber retained verbatim, restated at the point of edit.
- `金盘科技_深度研究报告_20260204.md` / `Jinpan_Technology_Deep_Research_Report_2026-02.md` — dated 2026-08-21 entry appended at the end of §9, before §10. Records the full H1 result, marks the two §9 "key monitoring metrics" that now read zero (SST order value) and negative (operating cash flow / net profit), and carries an explicit **valuation and price boundary** paragraph in both languages naming the retained 2026-07-31 / RMB 64.30 / ~RMB 29.6B / ~66x Q1-annualized caliber and stating plainly that its Q1-annualized denominator has been superseded by the issuer's own H1 disclosure.
- No `~~strikethrough~~` was introduced in either full-tier report; corrections are written as prose redlines.

---

## 3. Proposed — awaiting reviewer adjudication

### P-6 (NEW) — `jinpan-2026:receivables-cash-quality` should read `breached`, and that grade is stance-coupled

- **reportId / field:** `jinpan-2026`, `monitoring[receivables-cash-quality].reading` and `.readingAsOf`; consequentially `stance`, `conviction`, `stanceHistory`.
- **Proposed value:** `reading: "breached"`, `readingAsOf: "2026-08-21"`. This **cannot be applied by this track**, because it is stance-coupled — see below.
- **Evidence (primary):** Hainan Jinpan Smart Technology 2026 Semi-Annual Report, disclosed 2026-08-21 (unaudited, period ended 2026-06-30). Net cash flow from operating activities **RMB -23,762,688.51**, against **RMB +234,788,194.63** a year earlier, **-110.12% YoY**. The issuer's own risk-factor language: the negative figure *"widened the working-capital gap and raised funding-allocation pressure for the time being."* The issuer discloses the three-period series **2024: RMB -36.98M / 2025: RMB +600.97M / H1 2026: RMB -23.76M**. Net profit attributable to shareholders was RMB 303M, so operating cash flow / net profit is **negative** for the period against the report's own published ">70%" watch level.
- **Why the current published value is questionable:** the item currently carries **no grade at all** and a `latest` that predates the filing. Its own published trigger reads *"receivables collection and operating cash flow fail verification."* The operating-cash-flow half of that trigger has occurred on the issuer's own numbers and by the issuer's own characterisation.
- **Why it is not applied — the structural block.** `receivables-cash-quality` appears in **both** `stanceTriggers.upgrade.monitoringIds` and `stanceTriggers.downgrade.monitoringIds`, so it is a *linked* item. `jinpan-2026.stanceHistory` ends **2026-07-31**, earlier than the 2026-08-21 evidence date. Writing `breached` under those conditions puts the report on the Trigger Watch board via `breached_without_flip()` and fails `test_the_shipped_book_has_no_breached_without_flip`. Clearing it requires a stance move, which is a prohibited action for this track. This is the documented fail-closed path: the edit is withheld, the published fields are untouched, and the grade is handed over here.
- **The stance question the reviewer must decide.** The published `stanceTriggers.downgrade` is a **disjunction**: *"Move to cautious if AIDC growth falls below 100%, SST still has no large orders, overseas plants slip more than 6 months, gross margin falls below 24%, **or** receivables keep growing faster than revenue."* Quoted against the filing:
  - *"AIDC growth falls below 100%"* — **did not fire.** AIDC revenue +122.57% YoY.
  - *"SST still has no large orders"* — **FIRED.** No disclosed SST orders or revenue; prototype and overseas testing only.
  - *"overseas plants slip more than 6 months"* — **did not fire**, and resolved favourably: overseas backlog RMB 6.239B, +122.65% YoY, 58.32% of total.
  - *"gross margin falls below 24%"* — **did not fire.** 27.41%, +1.53pp YoY.
  - *"receivables keep growing faster than revenue"* — **not establishable.** Receivables plus contract assets +5.91% is *half-over-half*; revenue +12.73% is *year-over-year*. The bases are not comparable and the issuer supplies no H1-2025 receivables figure. This track makes **no finding** on this clause.

  The published `stanceTriggers.upgrade` is a **conjunction** and clearly did not fire (SST wins no orders; operating cash flow / net profit is negative, not >80%).
  So the honest structural reading is: **one downgrade clause fired, one is unestablishable, three resolved favourably, and the upgrade conjunction failed.** This is the same disjunctive-trigger shape as open item **O-5** (`mp-2026`), and it is offered to the reviewer in the same terms.
- **What would falsify P-6:** an audited or restated H1 cash-flow statement showing a different figure; or a reading of the trigger under which a *single* negative half-year — explicitly attributed by the issuer to advance stocking against backlog up 41.87% and to raw-material price lock-ins, with contract liabilities up 36.31% on higher order prepayments — does not constitute "fail verification" for an item whose companion clause (receivables collection) is not establishable this period.
- **Confidence, and what could not be verified:** high confidence on the arithmetic and on the issuer's own characterisation, both read directly from the filing. **Could not verify:** receivable days (not separately disclosed; not derived here); whether receivables outgrew revenue on a like-for-like basis; the CBP tariff-refund amount embedded in the gross-margin improvement (not disclosed), so its contribution cannot be separated.

### P-7 (NEW) — `jinpan-2026` valuation frame rests on a denominator the issuer has now superseded

- **reportId / fields:** `jinpan-2026` — `priceAsOf`, `stanceRationale`, `monitoring[annualized-pe].latest`, §8 and §10 of both Markdown files, and `prices.json` compatibility.
- **The concrete problem:** the published caliber is the **2026-07-31 close of RMB 64.30, ~RMB 29.6B market cap, ~66x P/E and ~4.9x P/S — with the P/E denominator being *Q1 2026 annualized*.** The semi-annual report has now supplied **actual H1** figures (revenue RMB 3.556B, net profit RMB 303M). A Q1-annualized denominator is superseded by an H1 actual on the issuer's own disclosure. The item's own `latest` text says in terms: *"the H1 report is the next test."* That test has arrived.
- **No multiple is proposed and none was computed.** Deriving a replacement P/E requires a current price, a current share count (the filing discloses **459,785,588** shares as of 2026-08-19, with 710,000 held in the repurchase account), a decision on annualisation convention, and prices/verdict compatibility — i.e. the complete per-report valuation package the reviewer has repeatedly said is the only thing that matures this class. This entry supplies the trigger and the share count, not arithmetic.
- **What would falsify P-7:** a demonstration that the published multiple band already absorbs the H1 result, such that the stance conclusion is unchanged on the new denominator.
- **Confidence:** high that the denominator is superseded; **no view offered** on what the rebuilt multiple would be or which direction it moves the stance.
- **Dependency:** P-6 cannot land without either P-7 or an independent stance decision, because the `breached` grade is blocked precisely by the unmoved stance.

**No other proposal is filed this run.** All seven previously open items were `deferred` by the 2026-08-20 review, and the contract forbids re-raising a deferred item on the very next run. They are carried forward unchanged in §4 and are **not** re-argued here — in particular **P-3** is not re-raised, even though the Nebius pricing and settlement terms named in its own tightened maturity condition have now appeared. That state change is recorded in §4 as a status note, not as a proposal.

---

## 4. Open items carried forward

Restated in full from memory (`research-radar-receipt-persistence-gap`), each re-verified against `origin/main` @ `8ad81b6` this run. **Seven carried in, all still open; two new (P-6, P-7) opened. None closed.**

- **O-3 — `coverage-map.json` has no rule for supplier-equity-in-customer structures.** Opened **2026-08-07**. Proposed `vendor-equity-in-demand`: *if chain suppliers (chip vendors, power providers, equipment makers) take equity or capital commitments in the vehicles that buy their own output, then treat the resulting order/PPA flow as partly self-funded and require third-party-funded demand confirmation before reading it as independent end-demand.* Trigger case: Helix Digital Infrastructure — NVIDIA strategic partner *and* investor, Vistra preferred power provider with an initial commitment of up to $1.0B, alongside KKR and the Kuwait Investment Authority. Reviewer `deferred` 2026-08-19 and again 2026-08-20, holding that PORTS-Pike is a **developer/tenant credit-support** structure, not the buyer-equity pattern the rule describes. Maturity is either **(a)** a second genuine supplier-equity-in-buyer instance, or **(b)** a rewritten falsifiable rule separating buyer equity from developer/tenant support. **Re-verified this run: `coverage-map.json` still holds 16 rules and contains no `vendor-equity-in-demand`.** Nothing this run matures it. Next known disclosure date: none determinable.
- **O-5 — `mp-2026:q2-2026-ppa-income` left ungraded; grade and stance coupled, both await adjudication.** Opened **2026-08-06** (evidence date). Fired clause, quoted from the item's own published trigger: *"or consolidated adjusted EBITDA falls below Q1 2026's US$36.6M"* → Q2 was **$28.493M**. Did **not** fire: the same trigger's first clause — product revenue replaced PPA roughly one-for-one and Materials ex-PPA went **-$5.5M → +$14.9M** — and the `stanceTriggers.downgrade` Q2 clause. Superseded but deliberately not recomputed: the `priceAsOf 2026-07-30` / $41.66 frame and every multiple on it (EV ~$7.19B, ~28.3x TTM revenue, ~141.8x TTM adj. EBITDA, ex-government TTM -$42.6M), stance `neutral-watch`, conviction `medium`. Reviewer `deferred` 2026-08-09 and re-affirmed through 2026-08-20. Maturity: the published `or` trigger is rewritten, **or** a complete current-price valuation and stance review adjudicates the mixed quarter. **Not met — re-verified in `main` this run: `reading` absent, `readingAsOf` absent, `priceAsOf` still 2026-07-30, `stanceHistory` still ends 2026-07-31 at $41.66.** Next known disclosure date: MP Q3, ~2026-11. **Note: P-6 above is structurally the same disjunctive-trigger problem on a different report.**
- **P-2 — opened 2026-08-13; the `applied-materials-2026` leg CLOSED 2026-08-14.** **Three** chain reports still carry valuation denominators their own prints superseded: `coherent-2026`, `coreweave-2026`, `nebius-2026`. Reviewer `deferred` 2026-08-14, 08-16, 08-19 and again 2026-08-20, with the explicit instruction not to repeat a price-only reminder and the maturity condition that a complete current-price valuation pass refresh price, share count, enterprise value, denominators, bilingual valuation text, prices/verdict compatibility and stance gates together **for the named report**. **Not met — re-verified in `main` this run: all three still carry `priceAsOf 2026-07-30`.** Evidence carried forward and not extended: Coherent's 10-K cover page discloses **195,832,246 shares outstanding at 2026-08-10**. ⚠️ **This run again edited `nebius-2026` monitoring and Markdown and again did not re-anchor it** — `priceAsOf` remains 2026-07-30 and §8's $188.43 caliber is retained verbatim, with both Markdown languages restating that boundary at the point of edit. The P-2 leg for `nebius-2026` is therefore **unchanged and still open**. New evidence recorded but **not** computed into any multiple: the priced offering adds $5.0B of original principal and the concurrent exchanges issue **approximately 15.8 million Class A ordinary shares**. **No multiple is proposed and none was computed.** Next known disclosure dates: Coherent FY2027 Q1 ~2026-11; CoreWeave Q3 ~2026-11; Nebius Q3 ~2026-11.
- **P-3 — opened 2026-08-14; `deferred` 08-14, 08-16, 08-19 and 2026-08-20.** `chain-debt-funded-capacity` rule gap. The 2026-08-20 review tightened the maturity condition to: *Nebius publishes pricing and settlement terms, or another issuer supplies a priced/closed named-capacity financing*; and then it must be proposed as a separate structural unit with schema, validator, page constants, docs and tests. **Status change this run, recorded but deliberately NOT re-raised: Nebius has now published pricing (0.50% 2030 / 4.50% 2034, $5.0B upsized) and a settlement date (2026-08-24, subject to customary closing conditions).** The contract forbids re-raising a `deferred` item on the very next run, and the tightened condition asks for a build rather than a receipt paragraph, so no proposal is filed. The reviewer's own observation still stands: the use-of-proceeds paragraph continues to list general corporate purposes alongside named capacity spend. **Re-verified this run: `coverage-map.json` still holds 16 rules and contains no `chain-debt-funded-capacity`.** Next known disclosure date: settlement, 2026-08-24.
- **P-4 — chain-wide price-anchor staleness.** Opened **2026-08-17** — the evidence date on which the moves were originally measured. **The opened date is deliberately NOT restated forward.** Reviewer `deferred` 2026-08-19 and again 2026-08-20, instructing that a price-only reminder must not be repeated and that it reopens **only** on a reviewer-adjudicated re-anchor sweep, a report-specific full valuation package, or proof that published multiple bands absorb the moves. **Accordingly this run does not restate the price table and computed no new deltas.** The last adjudicated figures stand as filed on 2026-08-19. Evidence remains `prices.json`, a repository artifact and not a primary source, usable only as a staleness indicator. **No multiple, market cap, EV or fair value has been computed by this run.** Nearest chain earnings that could discharge it, recomputed this run from `earnings-calendar.json` at `8ad81b6` (which was restandardised in `4bb3ec2`, *after* the 08-20 run's base, so the prior receipt's dates were not inherited): **2026-08-26** (Synopsys, NVIDIA, MiniMax) and **2026-08-27** (Marvell) — all four issuer-confirmed, day-precision.
- **U-2 — NVIDIA's $500B compute-financing platforms.** Opened **2026-08-10** (evidence date; never restated forward). Reviewer `deferred` 2026-08-16, 08-19 and again 2026-08-20: reopen on a final agreement, a named offtake, or a disclosed NVIDIA contribution inside the six-platform program. **Still not met.** The six platforms are with **Apollo, BlackRock, Blackstone, Brookfield, Goldman Sachs and KKR**. The binding reason U-2 stays unlogged is unchanged: **no `crossChecks` rule covers vendor-orchestrated third-party financing architecture** (`neocloud-financing` is the nearest and does not reach it), and the MoUs remain subject to execution of final agreements. Next known disclosure date: none determinable.
- **P-5 — opened 2026-08-18** (the evidence date — the Marvell warrant issuance — not a run date). Chain framework has no rule for customer warrants vesting on the customer's own purchases. Two instances, both quantified from primary sources: Astera Labs (three warrants, $2,097K Q1 2026 contra-revenue ≈ 0.7% of revenue) and Marvell/Google (up to 58,970,907 shares at $206.58, vesting in 240 tranches against each $500M of Custom Products revenue, purchases characterised by the issuer as discretionary). Reviewer `deferred` 2026-08-20 with the condition that it not be repeated **until Marvell discloses the warrant's accounting treatment or reports actual purchase-linked vesting/revenue effects**, and the explicit ruling that **Astera's contra-revenue treatment cannot be imputed to Marvell by analogy**. **Re-verified this run: `coverage-map.json` still holds 16 rules and contains no `customer-warrant-linked-revenue`.** ⚠️ Direction discipline retained: O-3 is supplier→buyer equity, P-5 is buyer→supplier equity; they must not mature each other. Next known disclosure date: **2026-08-27**, Marvell FY2027 Q2.
- **P-6 — NEW, opened 2026-08-21** (the evidence date — the Jinpan semi-annual report). `jinpan-2026:receivables-cash-quality` should read `breached`; blocked because the item is stance-linked and the stance has not moved. Full write-up in §3. What fired, quoted against the item's own published trigger *"receivables collection and operating cash flow fail verification"*: operating cash flow **RMB -23.76M** versus **RMB +234.79M**, **-110.12% YoY**, with the issuer itself reporting a widened working-capital gap. Superseded but deliberately not recomputed: nothing — the valuation side is carried separately as P-7. Next known disclosure date: **~2026-10** (2026 Q3 report, SSE statutory deadline 2026-10-31).
- **P-7 — NEW, opened 2026-08-21** (same evidence date). `jinpan-2026`'s published valuation caliber (2026-07-31 close RMB 64.30, ~RMB 29.6B market cap, ~66x P/E, ~4.9x P/S) rests on a **Q1-2026-annualized** earnings denominator that the semi-annual report has now superseded with actual H1 figures. Full write-up in §3. Specific figures superseded but deliberately not recomputed: the annualized P/E and P/S and the market-cap arithmetic behind them. Share count supplied for whoever performs the rebuild: **459,785,588** total shares as of 2026-08-19, less **710,000** in the repurchase account. `priceAsOf` remains 2026-07-31 and was not touched. Next known disclosure date: **~2026-10**.

**Previously closed and not re-raised:** O-1, O-2 (Oklo), O-4 (Vistra leverage, closed 2026-08-11), O-6 and P-1 (Vistra net-debt reproducibility — `rejected`, permanently closed), U-1 (SK hynix ₩54T, closed 2026-08-13), the `applied-materials-2026` leg of P-2 (closed 2026-08-14), and the **claimed `sk-hynix-2026:rerating-realized` cross-contamination flag** (raised 2026-08-20, `dropped` by the reviewer the same day: SEC CIK 2120882 Form 424B4 *is* SK hynix's official ADS offering and the 177.9M ADSs at $149 / ~$26.2B net proceeds figures are correct — closed, and the filer-name/CIK check is now a standing pre-condition before any cross-contamination claim).

---

## 5. Coverage

| Dimension | Count |
|---|---|
| Current chain reports checked | **47 / 47** |
| Cross-check rules checked | **16 / 16** |
| Monitoring items across those reports | **227** |
| Qualifying events found | **2** |
| Source failures | **0** |

**How the 47/47 was earned — stated plainly.** This is **not** 47 individual substantive reviews. It is:

1. **42 issuers** swept programmatically via `data.sec.gov/submissions/CIK{cik}.json`, one request per issuer, **0 failures**. The ticker list was generated from `reports.json`'s `priceSymbol` field in code, not typed by hand — the exhaustiveness check the reviewer required on 2026-08-19. `ALM` (`almonty-2026`) is absent from `company_tickers.json` and is resolved through a recorded manual CIK (`0001670061`); every other symbol resolved automatically. Every filing with `filingDate >= 2026-08-18` was pulled (85 filings), acceptance timestamps converted UTC→ET, and each in-window filing classified.
2. **4 non-EDGAR issuers** opened individually in the in-app browser: `jinpan-2026` (eastmoney SSE announcement list — **qualifying event found**), `sk-hynix-2026` (news.skhynix.com), `kioxia-2026` (kioxia-holdings.com/en-jp/ir.html), `minimax-2026` (etnet 0100.HK news).
3. **1 ETF** — `copx-2026` — has no issuer filings and no earnings entry; it is covered by construction, not by a filing check.
4. **Earnings-calendar cross-reference** against `earnings-calendar.json` at `8ad81b6`.

**Blind spots of that method, stated in the same breath.**

- **The earnings calendar covers 46 of the 47 chain reports** (only `copx-2026`, an ETF, has no entry). Of those 46, **24 carry a null or month-precision `expectedDate`** and are therefore covered **by the sweep alone**, with no dated calendar check backing them: `almonty-2026`, `alphabet-2026`, `amkr-2026`, `amzn-2026`, `ase-technology-2026`, `bloom-energy-2026`, `cadence-2026`, `constellation-energy-2026`, `corning-2026`, `digital-realty-2026`, `eqix-2026`, `jinpan-2026`, `kioxia-2026`, `kla-2026`, `lam-research-2026`, `meta-2026`, `microsoft-2026`, `neov-2026`, `oklo-2026`, `oracle-2026`, `pltr-2026`, `quanta-services-2026`, `vertiv-2026`, `vistra-2026`. **`jinpan-2026` is on that list, and this run's largest event came from it** — found by the issuer-page sweep, not by the calendar. That is the blind spot working as designed, and also a demonstration of how much it can hide.
- **No chain earnings fell inside the window.** Nearest are 2026-08-26 and 2026-08-27 (§4, P-4).
- **The 227 monitoring items were not each individually re-verified.** They were checked for deterministic due dates and for whether any in-window event supplied a new reading. Three received one.
- **EDGAR same-day tail:** filings accepted after 19:02 ET on 2026-08-20 are not in this window and pass to the next run. EDGAR accepts until 22:00 ET.
- **Non-EDGAR issuer pages show only what the issuer chooses to post**; for `minimax-2026` the etnet fallback can clear "no announcement" but is not an exchange filing feed. HKEX remains unreachable from this environment.

---

## 6. Unresolved candidates and items examined but not logged

| Item | Source / date | Disposition |
|---|---|---|
| **Applied Materials 10-Q** (FY2026 Q3, period ended 2026-07-26) | 10-Q accepted 2026-08-20 12:01 ET | **Not logged.** This is the periodic filing of the 2026-08-13 release already logged as `amat-q3-fy2026-record-revenue-dram-mix`. Compared item by item against the consumed release: the export-control content is explicitly "as previously disclosed" (BIS settlement of $253M dated 2026-02-11, paid in Q2 FY2026); no backlog is disclosed (the single "backlog" occurrence is forward-looking-statement boilerplate); no deferred-revenue disclosure. The one genuinely incremental fact is customer concentration — **two customers at approximately 20% and 14% of nine-month revenue**, no other above 10%, customers unnamed. That bears on no `crossChecks` rule and on none of AMAT's five monitoring items, so it does not qualify. Recorded here so the figure is not lost. |
| **SK hynix co-packaged-optics roadmap in *Nature Electronics*** | news.skhynix.com, 2026-08-20 | **Not logged** — falls squarely in the class the reviewer `rejected` on 2026-08-20: roadmap PR without a primary order, capacity, pricing or filed operating fact. In the new territory of this window, and named here for the audit trail. |
| **Nebius 6-K of 2026-08-19 07:48 ET; Marvell 8-K of 2026-08-19 08:25 ET** | EDGAR | Already logged by the 08-20 run; inside the 24h overlap only. Not re-logged. |
| **AMD 8-K (items 5.02, 9.01), Digital Realty 424B7 + 8-K (item 8.01), Amazon 424B3 + EFFECT** | EDGAR, 2026-08-18 16:10 → 2026-08-19 16:58 ET | All fall in the prior run's window and were adjudicated by the 2026-08-20 review — AMD governance `dropped`, Digital Realty resale registration `dropped`, Amazon registration mechanics `rejected` permanently. Not re-raised. |
| **73 Form 3/4/4A/5/144 filings in window** | EDGAR | Suppressed by rule; permanently-rejected classes. |
| **MiniMax block trades, Daiwa initiation, intraday price moves** | etnet, 2026-08-17 → 2026-08-20 | **Not logged** — permanently `rejected` classes (market-only items, analyst initiation). |
| **Kioxia** | kioxia-holdings.com | Nothing newer than 2026-08-10 (treasury-share status, major-shareholder change — both permanently-rejected classes, and both pre-window). |

**No unresolved candidate requires later review.** Every in-window item was either logged, matched to an existing reviewer disposition, or recorded above with its reason.

---

## 7. Sentinel status

**Everything in the sentinel is green.** Currency and metals both refreshed within 48h; all 56 research-price entries `ok` with none stale; 138 Python tests and 20 Node tests pass; all six validators pass; the feed regenerates idempotently; the package checker passes; the Hugo build succeeds. The only warnings anywhere in the stack are pre-existing baseline warnings on `salesforce-2026`, `coinbase-2026` and `sandisk-2026`, each reproduced against a clean `origin/main` worktree and none of them touched by this branch. Detail below.

### 7.1 Data freshness

All three green.

| Feed | Field | Value | Age at run time | Verdict |
|---|---|---|---|---|
| `static/invest/currency/data/historical.json` | `metadata.last_updated` | `2026-08-20T00:20:30` | ~30.7h | within 48h |
| `static/invest/metals/data/historical.json` | `metadata.last_updated` | `2026-08-20T01:31:45` | ~29.5h | within 48h |
| `static/invest/research/data/prices.json` | `generatedAt` | `2026-08-20` | 1 day | current |

`prices.json` holds **56 entries, all `status: "ok"`**, and **zero** entries carry a `lastDate` earlier than 2026-08-19 — no missing, stale or carried-forward research-price entry to flag. Nothing was repaired; this task does not autonomously touch the currency, metals or price-fetch pipelines.

### 7.2 Structural validation

This is a **data commit** (report content, signals and feed changed), so the full stack was run. Python 3.14.5.

| Check | Result |
|---|---|
| `python3 -m unittest discover -s static/invest/research -p "test_*.py"` | **OK — 138 tests** |
| `node --test report-module-parser.test.mjs test_tracking_rules.mjs` | **OK — 20 pass, 0 fail** |
| `validate_reports.py` | **OK — 69 reports** (3 pre-existing WARNs, see baseline note) |
| `validate_coverage_map.py` | **OK** — metadata, signal log and page wiring valid |
| `validate_prices.py` | **OK** |
| `validate_verdicts.py --strict-coverage` | **OK** |
| `static/invest/currency/validate_data.py` | **OK** |
| `static/invest/metals/validate_data.py` | **OK** |
| `generate_feed.py` | **OK** — feed.xml updated (14 insertions / 14 deletions); **second run produced no additional diff (idempotent)** |
| `check_research_package.py` | **OK — 69 reports** (2 pre-existing WARNs, see baseline note) |
| `hugo --minify --destination /tmp/alc-chain-radar-build-2026-08-21` | **OK — 8 pages, 267 static files, 101 ms** |
| `git diff --check` | **clean** |

**The `breached` gate specifically.** `test_the_shipped_book_has_no_breached_without_flip` (Python) and its Node mirror *"the real book currently has no breached-and-unreviewed report"* both **pass**. That is the check that would have failed had `jinpan-2026:receivables-cash-quality` been written as `breached`, and it is the direct reason P-6 is a proposal rather than an edit.

**Baseline warnings — reproduced against a clean `origin/main` worktree and confirmed pre-existing, not introduced by this branch:**

- `validate_reports.py`: `salesforce-2026` missing `priceSymbol` / `priceAsOf` / `reportedPeriod`; `coinbase-2026` missing `priceAsOf` / `reportedPeriod`. Identical on `origin/main` @ `8ad81b6`. These are non-chain reports and are tracked by the coverage-sentinel task, not this one.
- `check_research_package.py`: `sandisk-2026` zh/en depth warnings (body 54% / 56% of baseline). Byte-for-byte identical on `origin/main` @ `8ad81b6`. `sandisk-2026` was not touched by this run.

**Build noise handled.** The Hugo verification build rewrote `hugo_stats.json` even with `--destination` pointing outside the repository; it was restored with `git checkout -- hugo_stats.json` before staging and is **not** in this commit. The temporary build directory was removed.

---

## 8. Housekeeping performed this run

- `git fetch --prune origin`; one prior branch in this task's namespace: `codex/research-radar-2026-08-20` (producer commit `4455004`, reviewer commit `36af2f5` on top).
- **Review doc `docs/research-radar/2026-08-20-review.md` read in full before the scan.** It records "Merged to main as: 65d2139" and leaves the branch alive for downstream housekeeping.
- **Behaviour changed as a result, before scanning:**
  - The `within` this track wrote on `nebius-2026:funding-durability` was corrected to `unclear` by the reviewer, with the standing instruction that a proposed, unpriced offering grades `unclear` until priced/closed evidence exists. **That instruction is what made this run wait for pricing and then grade — it is directly responsible for §2.2.**
  - The `sk-hynix-2026:rerating-realized` cross-contamination flag raised on 08-20 was **wrong** — the reviewer opened SEC CIK 2120882 and confirmed the 424B4 is SK hynix's own ADS offering. New standing pre-condition adopted: open the cited primary filing and match filer name and CIK before alleging cross-report contamination. Flag closed.
  - Permanently-rejected classes extended (registration mechanics 424B3/EFFECT and resale registrations, roadmap PR and analyst commentary, routine convertible putback notices, market-only items). `dropped`-with-reason classes recorded as *keep scanning, raise the bar* rather than *stop* — capital allocation, resale registration, governance filings. Both directions were applied in §6.
  - All seven open items `deferred` → carried forward verbatim, **none re-raised this run**, including P-3 whose named maturity condition fired.
- **All dispositions folded into memory (`research-radar-receipt-persistence-gap`) BEFORE the branch was deleted.**
- **Branch deleted** — both consumed tests passed independently: (a) review doc present and its recorded producer commit matches the branch's newest non-review commit; (b) content spot-check — both branch signal ids (`nebius-convertible-senior-notes-offering-2026-08`, `marvell-google-custom-silicon-purchase-warrant-2026-08`) are present in `main`'s `signals.json`. `git push origin --delete codex/research-radar-2026-08-20` executed; local refs pruned.
- **No branch was left alive unprocessed.** The `codex/research-radar-*` namespace was empty at the start of this run's work. No branch outside this task's namespace was touched — `codex/coverage-sentinel-2026-08-20` belongs to another track and was left alone.
- **`docs/research-radar/README.md` rebuilt from scratch**, since it lives only on branches and has no ancestor in `main`. Historical rows are retained but de-linked, with a note that those branches were reclaimed.
