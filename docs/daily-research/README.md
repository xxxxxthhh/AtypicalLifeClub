# Daily Research Sentinel — 回执索引

合并轨（2026-08-31 起）每日运行的回执。**回执按设计不进 `main`** —— 复核人吸收数据后**保留分支并写下 review doc**，再由**次日运行（下游 producer）**读讫、折入 memory、然后删除分支；回执随分支一并消失。因此本索引只覆盖当前存活分支上的运行。

> **所有权（复核人 2026-09-07 要求写明，多轮再次点名；2026-09-19 review 的 TLDR-1 再次确认）：** review doc 的**消费**与日期分支的**删除**由**下游 producer**（本任务自身，即次日运行）负责，**不是复核人的行动项**；复核人不代删。
> 复核人写完 `YYYY-MM-DD-review.md` 后**保留分支**并标记 `AWAITING_DOWNSTREAM_HOUSEKEEPING`；次日运行读讫、把仍生效的裁决折进 memory，再按双测试删除分支。
> **⚠️ 删除前必须先把本 README 从待删分支取回**（它不在 `main` 里），否则索引会随分支永久丢失。
> **⚠️ 「历史」表只记录**已经删除**的分支；仍然存活的 ref 一律不进该表**（2026-09-19 review 点名：「README 的已删除章节不应收录仍存活 ref」）。

| 日期 | 分支 | 类型 | 计数 (NO_CHANGE / UPDATED / SOURCE_FAILURE) | 提案 | 回执 |
|---|---|---|---|---|---|
| 2026-09-26 | `codex/daily-research-2026-09-26` | **receipt only**（0 篇报告编辑、0 条 signal、0 条规则、0 个数据文件改动） | 63 / 0 / 0 = 63 | **1 项新增（P-20，bloom-energy-2026：main 已有的 Jupiter 不可抗力 signal 未挂 Bloom，报告未提 2.45 GW Jupiter）**；开放集 36 项全 deferred | [2026-09-26.md](2026-09-26.md) |

## 历史（分支已按「已消费」判定删除，回执随分支一并移除）

> **观察到的状态（非本轨动作）：** 长期挂账的 `codex/coverage-sentinel-2026-08-30` 自 2026-09-22 起**已不在 `origin`**。本轨未执行该删除。其 3 项待裁决若未包含在当前 32 项开放集内，则**只能由 memory 承载，分支已无法取回**。

| 日期 | 结局 |
|---|---|
| 2026-09-25 | receipt only，62/0/0，P-18 修订＋P-19 新开。复核 **`NO_RESEARCH_CHANGE`**（review commit `9277682`，producer tip `dd9c87c`，`Merged to main as: not merged`，标记 `AWAITING_DOWNSTREAM_HOUSEKEEPING`；174 项处置 ＝ deferred 92／taken (modified) 77／taken 3／dropped 2）：Vistra 发行候选、S-1 关闭；P-18／P-19 deferred（证据与结构门禁，不是授权问题）；「P-18 未来 TTM 必然下降」`dropped`；回执／README 进 main `dropped`。分支已于 2026-09-26 删除 |
| 2026-09-24 | receipt only，62/0/0，1 项新提案（P-18）。复核 **`NO_RESEARCH_CHANGE`**（review commit `f33daae`，producer tip `f8e49b4`，`Merged to main as: not merged`，标记 `AWAITING_DOWNSTREAM_HOUSEKEEPING`；131 项处置 ＝ deferred 69／taken (modified) 61／taken 1）：P-18、ONDS、STX 锚点三项 `deferred`；**62/62 不等于全事件阴性**；09 月 monitoring 为月级窗口、不称「已到期」；STX 修复须连同生成器与门禁（owner 随后于 main `835ecaf`＋`831904b` 完成）。分支已于 2026-09-25 删除 |
| 2026-09-23 | data run，60/0/0，1 项新提案（P-17）。复核 **`ACCEPT_WITH_CHANGES`**（review commit `d8fadf1`，producer tip `f6c1a64`，**Merged to main as `e68258e`**，标记 `AWAITING_DOWNSTREAM_HOUSEKEEPING`；143 项处置 ＝ taken (modified) 78／deferred 63／taken 1／dropped 1）：CoreWeave $4.2B 可转债 signal 由复核人独立读 8-K 后**精简重建**发布（删担保清单、受理时刻、其他发行的阴性陈述，补明潜在股数不是实际发行）；P-17、252 日极值、全部开放项与 21 条到期监测 `deferred`；回执／README 进 main `dropped`。分支已于 2026-09-24 按双测试删除 |
| 2026-09-22 | data run，59/1/0，0 项新提案。复核 **`ACCEPT_WITH_CHANGES`**（review commit `87c084d`，producer tip `2f8195f`，**Merged to main as `6808caf`**，标记 `AWAITING_DOWNSTREAM_HOUSEKEEPING`）：**只有收窄后的 Almonty signal 进了 main** —— `Almonty / bilingual Markdown`、`reports.json`、`producer feed` **全部 `dropped`**（英文 288,480,750 × 16.36 ＝ 4,719,545,070 与表内 4,353M 不符、同页混用约 70x/76x 与 08-14/08-17，缺陷在 main 既存，**不得随新 `lastUpdate` 重发旧结论**），监测更新 `deferred`；**`TLDR-2 / 排除脚本错误与 SEC 误标 Z 结论` `deferred`** —— 两段 hash 前缀与等长响应不能定位字段变化也不能排除解析错误，**只有「窗口边缘用申报自身 index header（ET）与原始 submissions 交叉核对、保留二者与冲突」的操作约束 `taken (modified)`，不得固定减 4 小时**；**52 周主张弱化** —— AMD `taken (modified)` 仅确认「收盘超过 API 所报上沿」，NRG `deferred`，升格需同口径且带明确截至日的完整 52 周序列；**18 个价格旗标 `taken (modified)`** —— 含 Kioxia 旧 bar，不得称 18 个当日新异动；**Python 版本 `taken (modified)`** —— producer 用 3.14.5 不合 3.11 要求，复核人全部重跑，今后须按 3.11 验证并记录真实版本；**`0 source failures` `taken (modified)`** —— 须区分**入口可达／正文可读／实质事实已审**三层，SEC Archives 403 与 cninfo 正文未读时不得报 0；**回执形状 `taken (modified)`** —— 应先结束证据复核再一次成形提交，TL;DR 置于开头；**回执/README `dropped`**（仍继续同时产出，两者都不进 main）。32 项开放项、21 条到期 monitoring、7 项候选逐条 `deferred`。分支已于 2026-09-23 按**测试 A ＋ B 双通过**删除（review doc 记载 producer `2f8195f` == 分支最新非 review 提交；且 signal id 已在 main 的 `signals.json`；本 README 已于删除前自待删分支取回） |
| 2026-09-21 | receipt only，59/0/0，0 项新提案。复核 **`NO_RESEARCH_CHANGE`**（review commit `e3bc7f1`，producer tip `458e963`，`Merged to main as: not merged`，标记 `AWAITING_DOWNSTREAM_HOUSEKEEPING`；**140 项处置 ＝ taken (modified) 74／deferred 64／taken 1／dropped 1**）：**EDGAR UTC 正对照是唯一的 `taken`** —— CRM 8-K `0001108524-26-000210` `acceptanceDateTime=2026-09-17T01:02:48.000Z` 按 UTC 解析再转 IANA 得 ET 09-16 21:02:48，与 `filingDate` 09-17 差一天，证实必须用 `acceptanceDateTime` 而非 `filingDate`；**NEOV 取证 `taken (modified)`** —— `/press-releases` 渲染索引有 `LOAD MORE`，只能说「当前可见索引在窗内未列新稿」，**不得称全站完整或全事件真阴性**，且 **EDGAR 与新闻稿是互补入口、EDGAR 不是所有实质事件的唯一准绳**；**SPA/sitemap `taken (modified)`** —— 外壳字节数本轮实测 3890（非固定 3878），不得把某字节数当常量、不得从有限样本断言所有路径永远相同；**HKEX `taken (modified)`** —— 须校验 HTTP 状态/内容类型/schema，失败不能当 0；**一次 404 不得认作永久失效**，UI 路线须留股票/日期/分页/结果证据；**float32 候选 `deferred`（未获永久结案）** —— 8/8 复现「与假说相容 ≠ 证明实际数据路径发生该转换」，须可保留的同窗上游响应＋依赖版本＋转换点＋阈值边界；**TSE 休市 `taken (modified)`** —— 以 JPX 官方日历取代「9 月第三个周一」推算，09-21/22/23 三日连休；**回执形状 `taken (modified)`** —— quiet-day 应先做完路线复查再只留一笔 receipt（09-21 出现 receipt＋更正两笔）；**回执/README 整合 `dropped`** —— 两份文件对分支协作仍有用，继续同时产出，两者都不进 main；**COIN 口径 `deferred`** —— 跳过条件是缺 `priceAsOf`，`reportedPeriod` 缺失是另一笔独立欠账。32 项开放项、21 条到期 monitoring、7 项候选逐条 `deferred`。分支已于 2026-09-22 按**测试 A** 删除（review doc 记载的 producer commit `458e963` == 分支最新非 review 提交，双向核对通过；删除时远端 tip 为 `e3bc7f1`；本 README 已于删除前自待删分支取回） |
| 2026-09-20 | receipt only，59/0/0，0 项新提案。复核 **`NO_RESEARCH_CHANGE`**（review commit `de402a9`，producer tip `dde2930`，`Merged to main as: not merged`，标记 `AWAITING_DOWNSTREAM_HOUSEKEEPING`；**138 项处置 ＝ taken (modified) 59／deferred 76／taken 1／dropped 2**）：**EDGAR 时区 `taken (modified)`** —— `acceptanceDateTime` 的 `Z` 须按 **UTC 解析再用 IANA `America/New_York` 转换**，**不是全年固定减 4 小时**（冬令时须自动处理），且原始字段与展示字段分开存；**yfinance vs Nasdaq 措辞收紧 `taken (modified)`** —— 只可称「不同取数服务／路径的核对」，**不可称「跨上游核对」**；**D-9 `deferred`** —— 旧「无转化披露」叙述确认陈旧，但 53M 是 binding capacity reservation、**不是已确认收入**，无完整包且无合格 crossCheck ⇒ 不单刷 `latest`／`lastUpdate`；**命名空间 `taken (modified)`** —— 永久范围**严格限 `codex/daily-research-*`** ＋ 三个精确 legacy 例外，**不批准 legacy 通配**；**回执/README `dropped`** —— 协调文档只能留在 producer 分支、不得进 main，历史栏须记录实际删除状态；**既有 breached（Salesforce／Spotify）`dropped`** —— 无新的已审读数不重复生成事件。NRG 52 周低、SNDK 0.0001、7 项候选及 32 项开放项逐条 `deferred`。分支已于 2026-09-21 按测试 A 删除（review doc 记载的 producer commit `dde2930` == 分支最新非 review 提交，双向核对通过；本 README 已于删除前自待删分支取回） |
| 2026-09-19 | data run，58/1/0，1 项新提案（P-16）。复核 **`REJECT`**（review commit `e59c33e`，producer tip `2779375`，`Merged to main as: not merged`；121 项处置＝dropped 8／deferred 54／taken (modified) 58／taken 1）：**`neov-2026` 整个生产包 `dropped`** —— 缺强制 monitoring signal、未同步 summary/highlights、`nextCheck` 过期、feed 以新日期重发旧非约束结论与旧估值，**四个生产文件整体不吸收**，收紧为「完整双语/metadata/feed/证据链包，不能只改 latest 及 lastUpdate」；**「无匹配 crossCheck ⇒ 可免 monitoring signal」`dropped`**（不匹配不是豁免授权，也不得强挂 `buildout-orders` 或默改 schema）；**Nasdaq「独立来源」与「舍入量级」措辞 `dropped`**（同 API 同源；仓库台账也须证明上游才可称独立；0.54 差额未查精度/时点不得叫舍入）；**阴性扫描与市场/申报截点 `dropped`**（宽窗有记录只证路由可返数据，不证窄窗穷尽；不认证「五市场一律最后交易日」与「EDGAR 统一 22:00 截点」；**不以周六为由免查会话**）；**回执/README `dropped`**（结束时间「09:4x」是占位符、历史栏目误称 09-18 已删除、旧内容 SHA 未全量更新 ⇒ 提交前固化实时时戳、实存 ref 与最终 SHA）；NEOV 关联方/「首次」/「无 8-K」穷尽性断言 `dropped`；价格分母三层与 COIN 行勾选、`primaryOutcome` ≠ 实质核验、提交形状（回执后又追加修表提交）均 `taken (modified)`；**结构性字段未违约 `taken`**（stance/conviction/priceAsOf/估值/prices/verdicts/schema 均未动）。P-16、D-8、MiniMax 中报冲突及 29 项开放项逐条 `deferred`。分支已于 2026-09-20 按测试 A 删除 |
| 2026-09-18 | data run，59/0/0，0 项新提案。复核 **`ACCEPT_WITH_CHANGES`**（review commit `7645582`，producer tip `63fd0a6`，**Merged to main as `a7a1f5f`**）：CoreWeave signal `taken (modified)`（仅保留独立读到的 8-K 事实，删除不可读 424B5 的分母）；**Almonty signal `dropped`**（「不要只因含 tungsten 或金额就匹配规则」）；**提交粒度 `dropped` —— 每个 signal id 一个独立提交、回执单独最后一提交**；**A-1 `taken (modified)`：`/historical` 与 `/chart` 同源，不得互称独立来源**；`limit` 缓存键机制 `deferred`（现象成立、机制未立）；价格腿分母须分三层；阴性覆盖措辞改为「所查来源未找到」；治理/程序类不挂 SOURCE_EVENT。分支已于 2026-09-20 按测试 A＋B 删除（09-19 那轮的删除曾被权限分类器拦截） |
| 2026-09-17 | receipt only，59/0/0。复核 `NO_RESEARCH_CHANGE`（review `77d2ce6`，producer `5b9edf3`）：**「改用 `/chart` 作默认收盘句柄」被否决** —— 优先带 ISO 日期参数的 `/historical` 指定日期行，缺行才用 `/chart` 并标临时；**固定下标 `ser[4]` 永久停用**；**TL;DR 必须置于任何正文说明之前**；「全宇宙无新事件」断言 `dropped`。分支已于 2026-09-18 删除 |
| 2026-09-16 | data run，58/1/0。复核 `ACCEPT_WITH_CHANGES`（**merged `b7cc69a`**）：MiniMax EGM 事实由复核人重建为**正文更正**发布，**本轨的 monitoring 改动被撤下（无合法 crossCheck 不能豁免 signal 门禁）**；「第 4 决议＝追加回购额度」永久 `rejected`；校验须用 Python 3.11。分支已于 2026-09-17 删除 |
| 2026-09-15 | receipt only，59/0/0，1 项新提案（P-15）。复核 `NO_RESEARCH_CHANGE`：**「`chain-wide-repricing` 两腿成立 ⇒ 共同原因」永久 `rejected`**；**「重锚价一律用 2026-09-11 收盘 $150.28」永久 `rejected`**；**「feed 无 diff ⇒ 报告未改」永久 `rejected`**；链报分母纠正为 **48 篇**；52 周极值断言 `dropped`。分支已于 2026-09-16 删除 |
| 2026-09-14 | receipt only，59/0/0，1 项提案（P-14）。复核 `NO_RESEARCH_CHANGE`：**quiet-day 两提交形状 `dropped`** —— 最终应只有一笔完整 receipt；来源清单只作 producer 运行日志。分支已于 2026-09-16 删除 |
| 2026-09-13 | receipt only，59/0/0，2 项提案。复核 **`ACCEPT_WITH_CHANGES`（本轨第一份 ACCEPT，115 项处置）**：I-1 关闭（CAS 准则由复核人重建发布 main `ecda98b7`，并纠正港股口径实为 IAS 34/IFRS）；`/historical` 需 ISO 日期参数（MM/DD 返 HTTP 200 ＋ 业务 400）。分支已于 2026-09-14 删除 |
| 2026-09-12 | data run，59/0/0，1 项提案。复核 **REJECT**：Oracle RPO 阶梯 signal 因复核人侧 10-Q 403 未能独立读取而不吸收（`deferred`，非永久否决）。分支已于 2026-09-13 删除 |
| 2026-09-11 | data run，57/2/0。复核 `ACCEPT_WITH_CHANGES`：TSMC `advanced-node-utilization` 由 `within` 改 `unclear`（main `a1bfbd6`）；Oracle signal 收窄后落 main `c0b2e31`；**Broadcom 整单元 `deferred`（未进 main ⇒ 新开 B-1）**。分支已于 2026-09-12 删除 |
| 2026-09-10 | data run，59/0/0。复核 **REJECT**（65 项）：ASE signal `dropped`（挂在已退役规则上）；**「未提交 8-K ⇒ 不重大」永久 `rejected`**；退役规则校验器缺口 `deferred`（＝ Q-1 仅剩的腿）。分支已于 2026-09-13 删除 |
| 2026-09-09 | receipt only，58/0/0，3 项提案。复核 `ACCEPT_WITH_CHANGES`：NeoVolta 财报日由复核人写入 main `4bdb001`；P-12 的「无 8-K ⇒ 不重大」永久 `rejected`。分支已于 2026-09-10 删除 |
| 2026-09-08 | receipt only，58/0/0。复核 `NO_RESEARCH_CHANGE`：金盘督导报告「新事实」其实已在正文；**feed 零 diff 推断 `dropped`**。分支已于 2026-09-10 删除 |
| 2026-09-07 | receipt only，58/0/0。复核 `ACCEPT_WITH_CHANGES`：确立「`earnings-calendar.json` 是 producer 可直接写的事实文件」。分支已于 2026-09-08 删除 |
| 2026-09-06 | receipt only，58/0/0。复核 `NO_RESEARCH_CHANGE`；Corning 会议日程 8-K `dropped`，立下「日程类只作发现」先例。分支已于 2026-09-07 删除 |
| 2026-09-05 | receipt only，58/0/0。复核 `NO_RESEARCH_CHANGE`：P-10 `deferred`；`$3.30` 行权价确已披露、增额权证 `727,273`/`727,272` 8-K 内部冲突。分支已于 2026-09-06 删除 |
| 2026-09-04 | data run，57/1/0。复核 `ACCEPT_WITH_CHANGES`：Broadcom Q3 重锚方向获认可但**授权路径被否决**，复核人独立重建后以 `72e557f` 发布。分支已于 2026-09-05 删除 |
| 2026-09-03 | data run，57/1/0。复核 `ACCEPT_WITH_CHANGES`，仅 Broadcom Q3 chain signal 并入 main（`baf958f`）。分支已于 2026-09-04 删除 |
| 2026-09-02 及更早 | 已由各自下游运行删除 |
